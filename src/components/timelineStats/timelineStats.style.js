import styled from "styled-components";

export const Wrapper = styled.div`
margin: 2.4rem 0.6rem 0rem 0.6rem;
background-color: ${(props) => props.theme.colors.secondary};
border-radius: 20px;
  box-shadow: 0px 10px 10px rgba(0, 0, 0, 0.2);
`